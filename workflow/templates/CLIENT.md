# 客户端实现：名称

> 解释客户端实际怎样实现功能；已有客户端手册继续沿用。替换提示与模板链接，不抄共同业务规则。

## 范围与依据

[功能说明](SPEC.md) · [服务端实现](SERVER.md) · [验证与验收](VERIFICATION.md)。列出负责的 AC、实际代码版本与已实现/拟实施状态；服务端不涉及时说明依据。引用协议、配置、生成来源与客户端代码入口。

## 界面与状态

说明入口、创建/展示/关闭、主要交互与状态变化；涉及加载、空数据、提交中、失败、断线或账号切换时说明表现及恢复条件。实际已有截图、界面文档与人工说明保留，避免逐控件照抄源码。

## 数据与调用链

按关键行为链接功能规则，串起事件 → 本地处理 → 请求/响应或推送 → 状态更新 → 界面刷新。写明本地缓存、持久数据与服务端数据分别由谁负责、何时同步或失效；引用协议原定义，不复制字段表。

列出实际文件与关键符号，说明各自作用及调用关系；生成文件链接到生成来源。涉及旧客户端、协议演进或存档时写兼容前提与发布顺序。

## 失败恢复与取舍

区分明确拒绝、网络错误和结果未知；说明等待/查询/重试条件、重复点击、迟到响应、界面关闭及重连后的处理。按实际影响补充权限展示、数据隔离、性能与资源释放；按钮隐藏不能代替服务端鉴权。

保留主要方案、选择理由和代价，未决定部分明确标注。当前项目已有机制优先复用，文档不凭空要求自建基础设施。

## 验证与维护入口

链接 [对应验证场景与实际结果](VERIFICATION.md)，标明当前实现缺口。后续行为变化时同步受影响的功能规则、共同契约、服务端处理与验证；只在本页记录必要的端内演进理由，跨端变更摘要留在功能入口。

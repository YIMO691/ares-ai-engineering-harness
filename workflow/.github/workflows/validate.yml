name: Validate workflow documentation

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Check Markdown structure and relative file links
        shell: pwsh
        run: ./scripts/validate-workflow.ps1

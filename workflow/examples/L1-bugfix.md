---
id: TASK-1042
title: 修复项目名称为空时列表页报错
level: L1
status: done
owner: 示例开发者
updated: 2026-09-01
---

# 目标

项目历史数据中名称可能为空，列表页格式化名称时发生空引用。页面应正常展示，并以“未命名项目”作为空名称占位。

# 改动范围

只调整列表展示层的空值处理，不修改历史数据、不改变创建项目时的名称校验。

# 验收标准

- [x] AC-01：名称为空的项目显示“未命名项目”，页面不报错。
- [x] AC-02：有名称项目的显示和排序不变。

# 验证与结果

- 新增空名称与正常名称单元测试，均通过。
- 本地打开包含两类数据的列表页，显示符合预期。

# Align Gate

`Pass`：最终行为与 TASK 的范围和 AC 一致，测试覆盖两类数据；本任务不需要独立对齐报告。

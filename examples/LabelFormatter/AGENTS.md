# Safe demo

Only edit src and tests for the requested task. Keep instructions and project configuration unchanged. Build outputs and test files must use the configured external task scratch. No commit, push or other repositories.

interface:
  display_name: "AEH Change Lens"
  short_description: "讲清 Unity 改动，并带你一步验证"
  default_prompt: "Use $aeh-change-lens to explain the current Unity change briefly, then give only the first verification action and its success signal."

policy:
  allow_implicit_invocation: false
